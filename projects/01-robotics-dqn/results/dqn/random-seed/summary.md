# Random-Seed Evaluations

Four recovered 50-episode files have success counts 38, 38, 35, and 34 under `score >= 10`. Each retained private file includes its own per-episode random seeds; the public version exposes aggregates only.
