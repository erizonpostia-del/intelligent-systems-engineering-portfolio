# Reproducibility record

## Public structure

```text
README.md
requirements.txt
src/
  ridge_lasso_experiment.py
  nested_cv_demo.py
results/
figures/
docs/
```

## Tested run

The public scripts were run successfully from the repository root on 2026-07-16 using Python 3.11.9. The verified packages were NumPy 2.4.6, pandas 3.0.3, matplotlib 3.11.0, and scikit-learn 1.9.0. `requirements.txt` declares compatible lower bounds rather than claiming to reconstruct the unavailable historical environment exactly.

```powershell
python -m pip install -r requirements.txt
python src\ridge_lasso_experiment.py
python src\nested_cv_demo.py
```

Both scripts use random seed 42. The Ridge/Lasso script generates `ridge_lasso_metrics.csv`, `ridge_lasso_summary.csv`, and three PNG figures. The nested-CV script generates `nested_cv_fold_results.csv`, `nested_cv_summary.csv`, and one PNG figure. All paths are relative to the repository root and output is limited to `results/` and `figures/`.

## Verification outcome

The two commands completed successfully in the public project directory. The current nested-CV mean outer-fold MSE is 250.367053 and the sample standard deviation is 35.478253, matching the migrated historical result files to displayed precision. The current Ridge/Lasso selected-model metrics also match the migrated historical values.

This is **basically reproducible, but lacks a historical dependency lock**. The original dependency manifest, original Git history, and persisted historical split indices were not available. Runtime data are generated by `make_regression`; the scripts do not read private archives, course files, external datasets, or any drive-specific path.
