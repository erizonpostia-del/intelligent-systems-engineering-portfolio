# Training Summary

The retained final training CSV includes 2,000 episode rows. It ends at global step 147,796 with replay buffer size 50,000 and includes 20 evaluation checkpoints. See the compact `training-summary.csv` at the parent directory.
